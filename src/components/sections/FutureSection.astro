---
import { future } from '../../content/siteContent';
---

<section id="future" class="section">
  <div class="container-prose">
    <h2 class="mb-8">{future.title}</h2>

    <div class="space-y-6 text-light-text/90 dark:text-dark-text/90">
      {future.paragraphs.map((paragraph) => (
        <p>{paragraph}</p>
      ))}
    </div>
  </div>
</section>
