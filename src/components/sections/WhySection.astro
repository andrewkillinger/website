---
import { why } from '../../content/siteContent';
---

<section id="why" class="section bg-light-surface dark:bg-dark-surface">
  <div class="container-prose">
    <h2 class="mb-8">{why.title}</h2>

    <div class="space-y-6 text-light-text/90 dark:text-dark-text/90">
      {why.paragraphs.map((paragraph) => (
        <p>{paragraph}</p>
      ))}
    </div>
  </div>
</section>
